version: '3'
services:
  frontend:
    build: ./frontend
    environment:
      - NODE_ENV=development
    volumes:
      - ./frontend:/usr/src/app
    command: sh -c 'PORT=8080 yarn start'
    ports:
      - 8080:8080
    tty: true
